"use client"

import { useState } from "react"
import { studyCenters } from "@/src/data/regional-network"

export default function StudyCenterDetails({
  params,
}: {
  params: { id: string }
}) {
  return <div></div>
}
